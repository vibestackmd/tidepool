/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { SPL_ACCOUNT_COMPRESSION_PROGRAM_ADDRESS } from "../programs/index.js";

export const CLOSE_EMPTY_TREE_DISCRIMINATOR = new Uint8Array([
  50, 14, 219, 107, 78, 103, 16, 103,
]);

export function getCloseEmptyTreeDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLOSE_EMPTY_TREE_DISCRIMINATOR,
  );
}

export type CloseEmptyTreeInstruction<
  TProgram extends string = typeof SPL_ACCOUNT_COMPRESSION_PROGRAM_ADDRESS,
  TAccountMerkleTree extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountRecipient extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountMerkleTree extends string
        ? WritableAccount<TAccountMerkleTree>
        : TAccountMerkleTree,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountRecipient extends string
        ? WritableAccount<TAccountRecipient>
        : TAccountRecipient,
      ...TRemainingAccounts,
    ]
  >;

export type CloseEmptyTreeInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type CloseEmptyTreeInstructionDataArgs = {};

export function getCloseEmptyTreeInstructionDataEncoder(): FixedSizeEncoder<CloseEmptyTreeInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CLOSE_EMPTY_TREE_DISCRIMINATOR }),
  );
}

export function getCloseEmptyTreeInstructionDataDecoder(): FixedSizeDecoder<CloseEmptyTreeInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCloseEmptyTreeInstructionDataCodec(): FixedSizeCodec<
  CloseEmptyTreeInstructionDataArgs,
  CloseEmptyTreeInstructionData
> {
  return combineCodec(
    getCloseEmptyTreeInstructionDataEncoder(),
    getCloseEmptyTreeInstructionDataDecoder(),
  );
}

export type CloseEmptyTreeInput<
  TAccountMerkleTree extends string = string,
  TAccountAuthority extends string = string,
  TAccountRecipient extends string = string,
> = {
  merkleTree: Address<TAccountMerkleTree>;
  /** Authority that controls write-access to the tree */
  authority: TransactionSigner<TAccountAuthority>;
  recipient: Address<TAccountRecipient>;
};

export function getCloseEmptyTreeInstruction<
  TAccountMerkleTree extends string,
  TAccountAuthority extends string,
  TAccountRecipient extends string,
  TProgramAddress extends Address =
    typeof SPL_ACCOUNT_COMPRESSION_PROGRAM_ADDRESS,
>(
  input: CloseEmptyTreeInput<
    TAccountMerkleTree,
    TAccountAuthority,
    TAccountRecipient
  >,
  config?: { programAddress?: TProgramAddress },
): CloseEmptyTreeInstruction<
  TProgramAddress,
  TAccountMerkleTree,
  TAccountAuthority,
  TAccountRecipient
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? SPL_ACCOUNT_COMPRESSION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    merkleTree: { value: input.merkleTree ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
    recipient: { value: input.recipient ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("merkleTree", accounts.merkleTree),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("recipient", accounts.recipient),
    ],
    data: getCloseEmptyTreeInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseEmptyTreeInstruction<
    TProgramAddress,
    TAccountMerkleTree,
    TAccountAuthority,
    TAccountRecipient
  >);
}

export type ParsedCloseEmptyTreeInstruction<
  TProgram extends string = typeof SPL_ACCOUNT_COMPRESSION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    merkleTree: TAccountMetas[0];
    /** Authority that controls write-access to the tree */
    authority: TAccountMetas[1];
    recipient: TAccountMetas[2];
  };
  data: CloseEmptyTreeInstructionData;
};

export function parseCloseEmptyTreeInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCloseEmptyTreeInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      merkleTree: getNextAccount(),
      authority: getNextAccount(),
      recipient: getNextAccount(),
    },
    data: getCloseEmptyTreeInstructionDataDecoder().decode(instruction.data),
  };
}
