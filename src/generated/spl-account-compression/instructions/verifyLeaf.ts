/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { SPL_ACCOUNT_COMPRESSION_PROGRAM_ADDRESS } from "../programs/index.js";

export const VERIFY_LEAF_DISCRIMINATOR = new Uint8Array([
  124, 220, 22, 223, 104, 10, 250, 224,
]);

export function getVerifyLeafDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(VERIFY_LEAF_DISCRIMINATOR);
}

export type VerifyLeafInstruction<
  TProgram extends string = typeof SPL_ACCOUNT_COMPRESSION_PROGRAM_ADDRESS,
  TAccountMerkleTree extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountMerkleTree extends string
        ? ReadonlyAccount<TAccountMerkleTree>
        : TAccountMerkleTree,
      ...TRemainingAccounts,
    ]
  >;

export type VerifyLeafInstructionData = {
  discriminator: ReadonlyUint8Array;
  root: ReadonlyUint8Array;
  leaf: ReadonlyUint8Array;
  index: number;
};

export type VerifyLeafInstructionDataArgs = {
  root: ReadonlyUint8Array;
  leaf: ReadonlyUint8Array;
  index: number;
};

export function getVerifyLeafInstructionDataEncoder(): FixedSizeEncoder<VerifyLeafInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["root", fixEncoderSize(getBytesEncoder(), 32)],
      ["leaf", fixEncoderSize(getBytesEncoder(), 32)],
      ["index", getU32Encoder()],
    ]),
    (value) => ({ ...value, discriminator: VERIFY_LEAF_DISCRIMINATOR }),
  );
}

export function getVerifyLeafInstructionDataDecoder(): FixedSizeDecoder<VerifyLeafInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["root", fixDecoderSize(getBytesDecoder(), 32)],
    ["leaf", fixDecoderSize(getBytesDecoder(), 32)],
    ["index", getU32Decoder()],
  ]);
}

export function getVerifyLeafInstructionDataCodec(): FixedSizeCodec<
  VerifyLeafInstructionDataArgs,
  VerifyLeafInstructionData
> {
  return combineCodec(
    getVerifyLeafInstructionDataEncoder(),
    getVerifyLeafInstructionDataDecoder(),
  );
}

export type VerifyLeafInput<TAccountMerkleTree extends string = string> = {
  merkleTree: Address<TAccountMerkleTree>;
  root: VerifyLeafInstructionDataArgs["root"];
  leaf: VerifyLeafInstructionDataArgs["leaf"];
  index: VerifyLeafInstructionDataArgs["index"];
};

export function getVerifyLeafInstruction<
  TAccountMerkleTree extends string,
  TProgramAddress extends Address =
    typeof SPL_ACCOUNT_COMPRESSION_PROGRAM_ADDRESS,
>(
  input: VerifyLeafInput<TAccountMerkleTree>,
  config?: { programAddress?: TProgramAddress },
): VerifyLeafInstruction<TProgramAddress, TAccountMerkleTree> {
  // Program address.
  const programAddress =
    config?.programAddress ?? SPL_ACCOUNT_COMPRESSION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    merkleTree: { value: input.merkleTree ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [getAccountMeta("merkleTree", accounts.merkleTree)],
    data: getVerifyLeafInstructionDataEncoder().encode(
      args as VerifyLeafInstructionDataArgs,
    ),
    programAddress,
  } as VerifyLeafInstruction<TProgramAddress, TAccountMerkleTree>);
}

export type ParsedVerifyLeafInstruction<
  TProgram extends string = typeof SPL_ACCOUNT_COMPRESSION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    merkleTree: TAccountMetas[0];
  };
  data: VerifyLeafInstructionData;
};

export function parseVerifyLeafInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedVerifyLeafInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 1) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 1,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { merkleTree: getNextAccount() },
    data: getVerifyLeafInstructionDataDecoder().decode(instruction.data),
  };
}
